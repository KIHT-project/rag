% chapter-07.tex
\chapter{Evaluation}

\section{Experimental Setup}

This chapter evaluates the platform as a set of separable capabilities: retrieval quality, answer generation quality, risk factor extraction quality, and operational reproducibility. The purpose of this separation is methodological clarity. A configuration may retrieve appropriate evidence but synthesize a weak answer, or it may generate a plausible answer while extracting an incomplete set of risk factors. Treating these outcomes as distinct prevents a single aggregate score from hiding different failure modes.
% evidence: evaluation_metrics/config/eval.yaml
% evidence: evaluation_metrics/src/phases/phase3_generate.py
% evidence: evaluation_metrics/src/phases/phase4_ragas.py
% evidence: evaluation_metrics/src/phases/phase6_extraction.py

The canonical run used for reporting is the primary deterministic run stored under the thesis evidence directory for 2026-02-09. Its configuration snapshot fixes the RAG API endpoint, the generation model, retrieval context size, RAGAS embedding model, extraction policy, and paper benchmark seeds. The primary deterministic condition uses seed 42 and temperature 0.0. A robustness condition is also present with five seeds at temperature 0.2, but the main chapter interpretation is based on the deterministic artifacts unless explicitly stated otherwise.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/config_snapshot.json
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/paper_benchmark_summary.json

The evaluation pipeline is phase based. Query records are read from a JSONL file, answers are generated for each enabled configuration, RAGAS metrics are computed from the generated answers and their recorded contexts, and extraction metrics are computed by comparing predicted risk factor sets against factors inferred from curated Label Studio tasks. This staged design supports reproducibility because each phase materializes inspectable intermediate outputs rather than relying on transient logs.
% evidence: evaluation_metrics/queries/queries.jsonl
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase3_answers_llm_only.jsonl
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase3_answers_rag_no_hyde.jsonl
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase3_answers_rag_hyde.jsonl

\section{Evaluation Dataset and Query Protocol}

The query protocol contains five thrombosis-oriented questions. The questions cover postoperative venous thromboembolism after total knee replacement, deep vein thrombosis after traumatic lower-extremity fracture, short peripheral catheter failure, pediatric surgery populations, and pneumonia cohorts before the COVID-19 period. The small size of this query set makes the experiment suitable for diagnostic comparison and thesis demonstration, but not for broad statistical claims about clinical performance.
% evidence: evaluation_metrics/queries/queries.jsonl

Each query is evaluated across all three answer-generation configurations. For the primary deterministic run, each configuration produced five answer records. This one-to-one structure allows per-query comparison across configurations, but it also magnifies the influence of individual difficult queries. Consequently, differences between configurations are interpreted as evidence about this benchmark run, not as stable estimates of population-level clinical validity.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase3_answers_llm_only.jsonl
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase3_answers_rag_no_hyde.jsonl
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase3_answers_rag_hyde.jsonl

The extraction evaluation uses curated task data as the reference substrate. Documents are retained when they are annotated as reporting risk factors with sufficient confidence under the configured policy, and factor strings are mapped into a closed canonical vocabulary. This design makes the extraction evaluation reproducible and inspectable, but it also means the reference labels are derived from abstract-level annotations and rule-based factor normalization rather than from a fully adjudicated clinical gold standard.
% evidence: evaluation_metrics/config/eval.yaml
% evidence: evaluation_metrics/src/phases/phase6_extraction.py

\section{System Configurations}

The first configuration is the LLM-only baseline. It sends the query directly to the generation model without retrieved contexts. This baseline tests the model's parametric response behavior and provides a comparison point for assessing whether grounding through the RAG service improves answer quality. Because no contexts are supplied, context-dependent metrics such as faithfulness and context precision are not applicable to this configuration.
% evidence: evaluation_metrics/src/phases/phase3_generate.py
% evidence: evaluation_metrics/src/phases/phase4_ragas.py

The second configuration is RAG without HyDE. This configuration routes each question through the biomedical RAG service and asks the service to synthesize an answer from retrieved evidence without query expansion. It tests whether the existing query text is specific enough to retrieve useful context directly. In this benchmark, this configuration is important because several questions are already specific to clinical populations and outcomes, which may reduce the need for expansion.
% evidence: evaluation_metrics/src/phases/phase3_generate.py
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase3_answers_rag_no_hyde.jsonl

The third configuration is RAG with HyDE enabled. HyDE adds a generation-mediated expansion step before retrieval, with the goal of improving semantic matching for difficult or underspecified queries~\cite{gao2023hyde}. In this experiment, HyDE tests whether expansion improves the retrieved evidence and downstream answers for thrombosis risk factor questions. Its expected benefit is higher semantic recall, while its expected risk is the introduction of expansion noise when the original query is already precise.
% evidence: evaluation_metrics/config/eval.yaml
% evidence: evaluation_metrics/src/phases/phase3_generate.py
% evidence: biomed_knowledge_platform/src/biomed_platform/core/services/hyde/hyde.py

\section{Retrieval Evaluation}

Retrieval evaluation asks whether the system ranks relevant biomedical evidence near the top of the result list. Normalized Discounted Cumulative Gain at k (nDCG-at-k) is relevant because it rewards systems that place more useful documents earlier, which matters when the answer generator consumes only a bounded number of contexts~\cite{jarvelin2002ndcg}. Precision at k measures how much of the top-ranked evidence is relevant, while recall at k measures how much of the known relevant evidence is recovered. These metrics evaluate retrieval quality before answer synthesis and therefore help distinguish ranking failure from later failures in answer generation or factor extraction.
% evidence: evaluation_metrics/src/phases/phase2_beir.py
% evidence: evaluation_metrics/src/phases/phase2_retrieval_weaklabels.py

The repository does contain retrieval-evaluation code, but its current state is narrower than a complete benchmark-ready ranking protocol. One path expects an explicit query-document relevance file in qrels format and computes mean reciprocal rank, precision at k, and recall at k. A second path derives weaker document-level relevance signals from the curated annotation export, for example whether a paper is labeled as related to venous thromboembolism. These capabilities show that retrieval measurement was planned, but they are not equivalent to a fully materialized, query-specific ranking evaluation for the canonical thesis run.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit
% evidence: evaluation_metrics/src/phases/phase2_beir.py
% evidence: evaluation_metrics/src/phases/phase2_retrieval_weaklabels.py

For the canonical run used in this thesis, the missing pieces are both procedural and evidential. The stored benchmark directory does not include the separate Phase 1 retrieval pool or Phase 2 retrieval outputs, so the reported run cannot be audited with an independent ranked list of retrieved documents. In addition, no query-specific gold relevance file was found for the five benchmark questions, and the current retrieval code does not implement nDCG-at-k. The available annotation labels are useful for weak supervision and overlap checks, but they are document labels rather than a complete set of judged relevant and non-relevant documents for each query. For these reasons, this chapter does not report numeric nDCG, retrieval precision, or retrieval recall values for the canonical run.

This limitation is material. Retrieval metrics would show whether an answer failed because the evidence was not retrieved, because the evidence was retrieved but not used faithfully, or because the extraction parser failed to recover the correct factor set. Without a canonical retrieval pool, query-specific relevance judgments, and corresponding Phase 2 outputs, the retrieval stage cannot be evaluated independently for the selected run. The chapter therefore treats retrieval quality as partially evaluated rather than fully measured.

\section{Generation and RAGAS Evaluation}

Generation quality is evaluated using Retrieval Augmented Generation Assessment (RAGAS) metrics~\cite{es2024ragas}. Answer relevancy estimates whether the response addresses the user's question. Faithfulness estimates whether the generated response is supported by the supplied contexts. The repository's context metric is the without-reference RAGAS variant, which estimates whether the retrieved contexts appear relevant enough to support the produced response without requiring a separate gold answer. These metrics matter because a biomedical assistant must answer the question, remain grounded in evidence, and avoid depending on irrelevant retrieved material.
% evidence: evaluation_metrics/src/phases/phase4_ragas.py

Answer relevancy is interpreted as user alignment rather than factual correctness. It asks whether the generated answer directly addresses the original question and penalizes answers that are incomplete, evasive, or padded with redundant detail. Operationally, the metric reverse-engineers multiple candidate questions from the generated answer and then measures the semantic similarity between those generated questions and the original user question. A simplified expression is shown in Equation~\ref{eq:answer-relevancy-score}.

\begin{equation}
\text{Answer Relevancy} =
\frac{1}{N} \sum_{i=1}^{N} \mathrm{sim}
(\text{Question}_{\text{original}}, \text{Question}_{\text{generated}, i})
\label{eq:answer-relevancy-score}
\end{equation}

This interpretation is important because answer relevancy and faithfulness capture different failure modes. An answer can be highly relevant to the user's prompt while still being unfaithful to the retrieved evidence. In other words, a response may be well targeted and persuasive while remaining unsupported by the indexed biomedical context.

Faithfulness is interpreted as groundedness, meaning whether the generated answer is derived only from the retrieved context rather than from unsupported model inference. Operationally, the metric treats the answer as a set of individual statements and checks whether each statement can be inferred from the retrieved passages. A simplified expression of that logic is shown in Equation~\ref{eq:faithfulness-score}.

\begin{equation}
\text{Faithfulness Score} =
\frac{\text{Number of answer statements supported by the context}}
{\text{Total number of statements in the generated answer}}
\label{eq:faithfulness-score}
\end{equation}

This interpretation is important for biomedical use. A high faithfulness score indicates low hallucination risk because the answer remains tied to retrieved evidence, but it does not by itself guarantee that the answer is clinically useful, complete, or fully responsive to the original question. An answer may be perfectly grounded in the supplied context and still fail to address the user's real information need.

Context precision requires more careful interpretation. In classical retrieval evaluation, precision at rank is anchored to whether the retrieved items are relevant to the original question, and higher values indicate that the top of the ranked list is populated with useful evidence. The without-reference RAGAS variant used in this repository does not use an explicit gold relevance set. Instead, it estimates whether the recorded contexts appear useful for supporting the produced answer. It is therefore not a pure retriever-only metric, because the judgment is mediated by the generated response rather than by an independent query-document relevance standard. As a result, it is best treated here as a retrieval-support proxy rather than as a definitive ranking metric such as nDCG or manually adjudicated precision at k.

A simplified ranking-style view of context precision is shown in Equation~\ref{eq:context-precision-score}, which captures the general idea that relevant chunks should appear early in the retrieved list.

\begin{equation}
\text{Context Precision@K} =
\frac{\sum_{k=1}^{K} P@k \times I(k)}
{\text{Total number of relevant chunks in top } K}
\label{eq:context-precision-score}
\end{equation}

This distinction matters methodologically. In the current repository, RAGAS Context Precision should be interpreted as an indirect indicator of retrieval support quality, not as a formal retrieval-ranking result. Metrics such as nDCG, Precision@k, and Recall@k require two explicit inputs for each benchmark question: a ranked list of retrieved documents and relevance judgments, that is, a record of which documents should count as relevant to that specific query. The canonical thesis run stores generated answers, cited snippets, RAGAS scores, and extraction outputs, but it does not include a separate Phase 1 retrieval pool or Phase 2 retrieval evaluation for that run. The repository also contains document-level annotation labels and partial retrieval-evaluation code, but these are not yet a complete query-specific gold standard for the reported benchmark. For this reason, Context Precision is useful for showing whether the context supporting an answer appears focused and helpful, but it should not be interpreted on its own as definitive evidence that retrieval ranking quality has been measured directly.

\begin{table}[h]
\centering
\caption{Mean RAGAS metrics by configuration for the selected run.}
\label{tab:ragas-metrics-summary}
\input{resources/tables/ragas_metrics_summary}
\end{table}

Table~\ref{tab:ragas-metrics-summary} reports the generated RAGAS summary table available in the thesis resources. In that table, RAG without HyDE has higher answer relevancy than RAG with HyDE, and it also has stronger faithfulness and context precision. The LLM-only baseline has answer relevancy but no faithfulness or context precision values, because no retrieved contexts are provided to evaluate grounding. The result does not support a blanket claim that HyDE improves generation quality in this benchmark.
% evidence: docs/thesis/latex/src/resources/tables/ragas_metrics_summary.tex

\begin{figure}[h]
\centering
\includegraphics[width=0.8\linewidth]{resources/images/ragas_metrics_summary.pdf}
\caption{Visual summary of mean RAGAS metrics by configuration.}
\label{fig:ragas-metrics-summary}
\end{figure}

The canonical paper benchmark summary is treated as the authoritative aggregate artifact for the deterministic run. In that summary, RAG without HyDE reports answer relevancy of 0.8672, faithfulness of 0.8167, and context precision of approximately 1.0000, whereas RAG with HyDE reports answer relevancy of 0.8202, faithfulness of 0.6458, and context precision of approximately 0.9000. The likely explanation is not that HyDE is intrinsically inferior, but that these five queries are already specific enough that expansion can introduce less relevant context or alter the evidence mix.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/paper_benchmark_summary.json

\section{Risk Factor Extraction Evaluation}

Risk factor extraction is evaluated separately from general answer quality. The extraction phase compares the system's predicted risk factor set with a gold factor set derived from curated task annotations and cited documents. Crucially, the Phase 6 extraction evaluation measures medical factor extraction conditionally on citation; a risk factor is only considered correctly extracted if it is explicitly linked to a valid citation in the generated JSON schema. The evaluation relies on a strict keyword-matching approach against a canonical vocabulary (\texttt{DEFAULT\_CANONICAL\_FACTORS}), mapping varied clinical phrasing back to standard terms. True positives count correctly recovered factors, false positives count predicted factors absent from the reference set, and false negatives count reference factors missed by the system. Precision captures over-extraction, recall captures missed factors, and F1 balances the two. This distinction is important because a response can sound relevant while still omitting clinically meaningful risk factors or adding unsupported ones.
% evidence: evaluation_metrics/src/phases/phase6_extraction.py

\begin{table}[h]
\centering
\caption{Mean extraction metrics by configuration for the selected run.}
\label{tab:extraction-metrics-summary}
\input{resources/tables/extraction_metrics_summary}
\end{table}

Table~\ref{tab:extraction-metrics-summary} reports macro-level extraction behavior across the five evaluated queries. RAG without HyDE has higher recall and F1, while RAG with HyDE has slightly higher precision. This indicates a precision-recall tradeoff. HyDE produced fewer false positives on average, but it also missed more gold factors. RAG without HyDE recovered all gold factors in the evaluated set, but with a modest increase in false positives.
% evidence: docs/thesis/latex/src/resources/tables/extraction_metrics_summary.tex

\begin{table}[h]
\centering
\small
\setlength{\tabcolsep}{3pt}
\caption{Micro level extraction error profile by RAG configuration.}
\label{tab:extraction-error-profile}
\begin{tabular}{lrrrrrrp{5.1cm}}
\toprule
Configuration & TP & FP & FN & Precision & Recall & F1 & Interpretation \\
\midrule
RAG without HyDE & 30 & 6 & 0 & 0.8333 & 1.0000 & 0.9091 & Higher recall, no missed gold factors, but more false positives. \\
RAG with HyDE & 29 & 5 & 2 & 0.8529 & 0.9355 & 0.8923 & Slightly cleaner predictions, but missed two gold factors. \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:extraction-error-profile} summarizes the run-level micro counts and confirms the main practical tradeoff. RAG without HyDE is preferable when sensitivity and complete factor recovery are prioritized, whereas HyDE is slightly more conservative. In biomedical risk factor extraction, false negatives may be more problematic when the system is used for evidence screening, while false positives primarily increase human review burden.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase6_extraction_phase3_answers_rag_no_hyde_summary.csv
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase6_extraction_phase3_answers_rag_hyde_summary.csv

\begin{figure}[h]
\centering
\includegraphics[width=0.8\linewidth]{resources/images/extraction_metrics_summary.pdf}
\caption{Visual summary of risk factor extraction metrics by configuration.}
\label{fig:extraction-metrics-summary}
\end{figure}

\section{Operational Evaluation}

Operational evaluation concerns whether the experiment can be traced, repeated, and audited. The primary indicators in this thesis are configuration snapshots, deterministic seed control, per-phase materialized outputs, and separate deterministic and robustness run directories. These properties matter because biomedical RAG evaluation is sensitive to generation settings, evaluator behavior, retrieved contexts, and corpus state. Without run traceability, metric differences cannot be confidently attributed to configuration differences.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/config_snapshot.json
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/paper_benchmark_summary.json

The canonical run preserves generated answers, RAGAS outputs, diagnostics, extraction per-query outputs, extraction summaries, and aggregate paper benchmark summaries. The deterministic run uses a fixed seed and temperature 0.0, while the robustness condition records five stochastic seeds at temperature 0.2. This structure supports operational reproducibility at the artifact level. The principal limitation is that reproducibility is not equivalent to identical future model behavior: external model serving, vector store state, and evaluator implementation details can still affect outputs if the run is reproduced in a changed environment.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/robustness

\section{Results}

The main result is that RAG without HyDE performs best overall on this small deterministic benchmark. It achieves the strongest reported answer relevancy among the RAG configurations, the strongest faithfulness and context precision in the canonical summary, and the highest extraction recall and F1. The LLM-only baseline is useful as a control for parametric answering, but it cannot be evaluated for grounding because no retrieved contexts are supplied. Therefore, its answer relevancy score should not be interpreted as evidence of source-supported biomedical reasoning.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/paper_benchmark_summary.json

\begin{table}[h]
\centering
\caption{Auxiliary flattened metric snippet from the paper benchmark summary.}
\label{tab:paper-benchmark-summary}
\input{resources/tables/paper_benchmark_summary_metrics}
\end{table}

Table~\ref{tab:paper-benchmark-summary} is retained as an auxiliary generated snippet, but it should not be read as a full configuration comparison because the exported table does not include the configuration name for each value. The full interpretation therefore relies on the canonical JSON summary and the mode-specific extraction summaries reported above. Those artifacts support a cautious conclusion: HyDE did not improve this benchmark run overall, and the no-HyDE configuration should be treated as the stronger configuration for these five diagnostic questions. The result is plausible because the questions are narrow and clinically explicit, so generated query expansion may add context that is semantically related but less directly aligned with the desired answer.
% evidence: docs/thesis/latex/src/resources/tables/paper_benchmark_summary_metrics.tex
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/paper_benchmark_summary.json

The robustness section of the benchmark summary shows stable RAG answer relevancy, context precision, and extraction metrics across the five stochastic seeds, while LLM-only answer relevancy varies more substantially. This suggests that the recorded RAG outputs were less sensitive to the sampled generation seeds in this run than the LLM-only baseline. However, because the benchmark contains only five questions, the robustness result should be interpreted as a sanity check rather than as a statistical stability claim.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/paper_benchmark_summary.json

\section{Error Analysis}

The strongest observed error pattern is expansion noise. In the HyDE configuration, query expansion sometimes retrieved or emphasized contexts that were related to the broad disease area but less aligned with the exact question. This is visible in the lower context precision and faithfulness reported for HyDE relative to no-HyDE, and in the two false negatives in the extraction summary. The pediatric surgery query is a representative case: HyDE produced lower extraction recall for that query than the no-HyDE configuration, indicating that some reference factors were missed.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase6_extraction_phase3_answers_rag_hyde_per_query.csv
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase6_extraction_phase3_answers_rag_no_hyde_per_query.csv

A second error pattern is over-extraction. RAG without HyDE achieved perfect extraction recall in the evaluated queries, but it did so with six false positives at micro level. This suggests that the configuration tends to preserve more candidate factors from the cited context or generated summary, including factors that are not present in the derived gold set for that query. In biomedical settings, this is a meaningful tradeoff: high recall reduces missed risk factors, but unsupported or irrelevant factors can reduce trust and may require human review.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase6_extraction_phase3_answers_rag_no_hyde_summary.csv

The LLM-only baseline demonstrates a different risk. It can produce relevant-looking answers without retrieved contexts, but those answers are not grounded in the indexed corpus for the experiment. The evaluation therefore cannot distinguish correct parametric knowledge from unsupported synthesis using context-based RAGAS metrics. This reinforces the architectural decision to evaluate generation quality and grounding separately.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/primary_deterministic/phase4_ragas_phase3_answers_llm_only.csv

\section{Threats to Validity}

The first threat is dataset size. The primary benchmark contains five questions, which is sufficient for inspecting pipeline behavior but not sufficient for robust statistical comparison. A single difficult query can materially shift means, especially for extraction metrics where factor counts vary by cited document. Future evaluation should expand the query set before drawing stronger conclusions.
% evidence: evaluation_metrics/queries/queries.jsonl

The second threat is reference construction. The extraction gold set is derived from curated task annotations, confidence thresholds, cited DOI alignment, and canonical factor matching rules. This creates a reproducible evaluation substrate, but it is not the same as a fully adjudicated expert gold standard. The results therefore support system evaluation under the repository's annotation protocol, not clinical validation.
% evidence: evaluation_metrics/config/eval.yaml
% evidence: evaluation_metrics/src/phases/phase6_extraction.py

The third threat is incomplete retrieval evaluation. Although retrieval metric code exists, the canonical run does not include the phase outputs required to report nDCG at k, retrieval precision at k, or retrieval recall at k. This limits the ability to isolate ranking quality from synthesis and extraction behavior. The present chapter therefore reports retrieval evidence conservatively and identifies the missing ranking artifacts as an unresolved evidence gap.
% evidence: evaluation_metrics/src/phases/phase2_beir.py
% evidence: evaluation_metrics/src/phases/phase2_retrieval_weaklabels.py

The fourth threat is evaluator dependence. RAGAS scoring relies on an evaluator model and embedding model configured for the run. The configuration snapshot records these choices, which improves traceability, but evaluator judgments may vary across model versions or runtime environments. This is particularly relevant for faithfulness and answer relevancy, where evaluator behavior can influence numeric scores.
% evidence: evaluation_metrics/config/eval.yaml
% evidence: evaluation_metrics/src/phases/phase4_ragas.py

A practical mitigation for this threat is evaluator calibration against human judgment. A stronger future protocol should expand the query set substantially and then manually review a representative sample of generated answers and retrieved contexts, comparing expert scores with the automated RAGAS outputs for answer relevancy, faithfulness, and context precision. Correlation or agreement analysis would make it possible to estimate how well the automated evaluator matches domain judgment in this biomedical setting and whether metric thresholds need adjustment before stronger claims are made.

\section{Summary}

The evaluation shows that the platform can execute a traceable, multi-phase biomedical RAG benchmark across LLM-only, RAG without HyDE, and RAG with HyDE configurations. The strongest configuration in the canonical deterministic run is RAG without HyDE. It reports stronger RAGAS grounding behavior than HyDE in the available summaries and achieves higher extraction recall and F1, while HyDE achieves slightly higher extraction precision but misses more gold factors.
% evidence: evaluation_metrics/runs/old/feb-10-valid-1-full-run/20260209_114745_nogit/paper_benchmark_summary.json

The result should be interpreted narrowly. The experiment demonstrates reproducible evaluation plumbing and provides diagnostic evidence about the current configuration set, but it does not establish clinical validity. The main unresolved quantitative gap is the absence of canonical retrieval ranking artifacts for nDCG at k, retrieval precision at k, and retrieval recall at k. Addressing that gap would strengthen the separation between retrieval, generation, extraction, and operational quality in future thesis iterations.
