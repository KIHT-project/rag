% chapter-05.tex
\chapter{Methodology}

\section{Study Design}

This chapter defines the methodological protocol used to connect system construction with evaluable evidence. The study design does not treat the platform solely as a retrieval service; instead, it examines a composite workflow in which automated acquisition, manual abstract review, ingestion control, and experimental evaluation contribute different forms of validity. This framing is necessary because system quality depends not only on retrieval metrics, but also on whether the indexed corpus can be curated in a traceable and reproducible manner.
% evidence: docs/THESIS_PLAN.md
% evidence: docs/thesis/STYLE_GUIDE.md
% evidence: docs/thesis/evidence/CLAIMS.md

Methodologically, the architecture supports a staged form of validation. Automated services provide acquisition, indexing, and answer generation behavior, while the Label Studio curation workflow supplies manually reviewed task records that can be exported, enriched, and selectively handed to downstream ingestion. The design decision is to separate curation from online retrieval so that review judgments and metadata-completeness checks refine the evidence base without being conflated with query-time behavior. This supports clearer interpretation of later evaluation outcomes, although it does not by itself constitute clinical validation.
% evidence: labelstudio-tools/src/app/python/download_data/main.py
% evidence: labelstudio-tools/src/app/python/rag_importer/rag_integration.py

\section{Dataset Protocol}

The dataset protocol combines two complementary sources of evidence. First, biomedical documents enter the platform through scheduler-driven or direct ingestion workflows. Second, an annotation-oriented curation stream prepares abstract-level tasks for manual biomedical review in Label Studio. In the task-preparation stage, the \texttt{labelstudio-tools} package reads a structured Excel source, checks that required bibliographic columns are present, normalizes selected boolean review flags, optionally applies exclusion-oriented filters, and emits task payloads containing PMID, title, and abstract content.
% evidence: labelstudio-tools/src/app/python/config.py
% evidence: labelstudio-tools/src/app/python/data_loader.py
% evidence: labelstudio-tools/src/app/python/filters.py
% evidence: labelstudio-tools/src/app/python/tasks.py

After export from Label Studio, the reviewed task set is enriched with DOI and related source metadata and then partitioned into separate artifact classes. The repository contains raw exported tasks, a clean dataset prepared for ingestion, a rejected-task report, and a file listing source identifiers for unresolved DOI enrichment cases. In the current artifact snapshot, 1300 tasks are present in the raw export, 1288 are retained in the clean export, and 12 are listed as rejected due to missing ingest-critical metadata. This partitioning supports reproducibility because later ingestion and evaluation steps can be tied to explicit intermediate datasets rather than reconstructed retrospectively.
% evidence: labelstudio-tools/src/app/python/download_data/main.py
% evidence: labelstudio-tools/ls_data/tasks_raw.json
% evidence: labelstudio-tools/ls_data/tasks_clean.json
% evidence: labelstudio-tools/ls_data/tasks_rejected.json
% evidence: labelstudio-tools/ls_data/missing_source_ids.json

\section{Annotation-Based Validation}

Annotation-based validation is used here as a curation and evidence-screening mechanism rather than as an experimental endpoint with claimed clinical outcomes. Exported Label Studio tasks retain manual annotation objects together with the underlying abstract text and enriched document identifiers. The observed task structure includes review signals such as inclusion evidence spans, relation-to-venous-thrombosis choices, confidence ratings, risk-factor reporting choices, article-type choices, and category selections. These annotations provide structured human judgments that can be inspected alongside the indexed source records.
% evidence: labelstudio-tools/ls_data/tasks_raw.json

The methodological role of these curated tasks is twofold. First, they create a traceable bridge between manual screening activity and the subset of records eligible for RAG ingestion. Second, they provide a review-oriented substrate that can support later evaluation design, for example by identifying papers considered related to thrombosis or likely to contain risk-factor evidence. However, the current repository evidence does not establish inter-annotator agreement, annotation completeness guarantees, or a clinical adjudication protocol. Those limitations must therefore be preserved explicitly when interpreting any downstream findings.
% evidence: labelstudio-tools/src/app/python/download_data/main.py
% evidence: docs/thesis/evidence/CLAIMS.md

\section{Reproducibility Protocol}

Reproducibility is supported through the project's repository-aware Engineering Wiki philosophy, configuration-driven execution, and durable intermediate artifacts. The curation workflow derives its behavior from environment variables controlling project identifiers, sheet selection, filter activation, export destinations, ingestion batch size, timeout values, and retry parameters. The export and ingestion stages both materialize inspectable files or request batches, which makes it possible to reconstruct which reviewed records were eligible for downstream import and which were excluded.
% evidence: labelstudio-tools/README.md
% evidence: labelstudio-tools/src/app/python/config.py
% evidence: labelstudio-tools/src/app/python/download_data/save_data.py
% evidence: labelstudio-tools/src/app/python/rag_importer/rag_integration.py
% evidence: docs/thesis/STYLE_GUIDE.md

This repository-aware protocol improves methodological transparency by preserving raw, clean, rejected, and missing-identifier artifacts as separate states in the curation lifecycle. Evaluation runs also preserve configuration snapshots, which supports traceability between metric outputs and the settings used during execution. These mechanisms improve reproducibility at the data-processing and artifact-audit levels, but they do not guarantee byte-identical future behavior because model runtime state, vector-store state, and corpus state may change. The main limitation is that some workflow assumptions remain external to the code, including the operational practice by which reviewers complete annotations before export. Consequently, certain governance properties of the review process still require procedural documentation beyond the repository.
% evidence: labelstudio-tools/ls_data/tasks_raw.json
% evidence: labelstudio-tools/ls_data/tasks_clean.json
% evidence: labelstudio-tools/ls_data/tasks_rejected.json
