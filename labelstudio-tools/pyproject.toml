[project]
name = "ls-importer"
version = "0.0.1"
description = "Excel to Label Studio task importer"
requires-python = ">=3.11"

[project.scripts]
ls-import = "ls_importer.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
